const connectDatabase = require("./connectDatabase");
const getCompression = require("./getCompression");
const encrypt = require("./encrypt");

module.exports = { connectDatabase, getCompression, encrypt };
